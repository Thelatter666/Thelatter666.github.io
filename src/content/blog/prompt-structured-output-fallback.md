---
title: 让大模型稳定输出 JSON 的四层容错
description: 大模型经常在 JSON 外面包 markdown 代码块、字段名写错、类型不一致。记录我做 AI 旅行规划助手时的容错链路设计。
pubDate: 2026-08-29
tags: ['Prompt 工程', 'LLM', 'FastAPI']
draft: true
---

做 AI 旅行规划助手时，我要让模型输出严格结构的行程 JSON：按天分组，
每天有主题和若干活动，活动带时段、地点、描述、交通方式。

但实测下来，模型的输出很难直接用。

## 它会怎么"不乖"

（正文待本人补充：实测遇到的具体异常形态 —— markdown 代码块包裹、
字段名不一致、类型写成字符串、缺字段等，各出现频率如何。）

## 四层容错链

（正文待本人补充：四层分别是什么、顺序为什么这样排、每层的实现要点。）

## 降级设计

（正文待本人补充：API Key 未配置时降级 Mock 的考虑 —— 是为了本地开发方便，
还是为了线上兜底？）

---

> 本文为种子草稿，正文待本人核实补充后再发布。
